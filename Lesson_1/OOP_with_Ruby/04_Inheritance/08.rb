=begin
  
The method is raised, because the method `hi` is a private method. Private
methods can only be accessed by other methods in the class, but can't be called
on an object instance.

To fix it, simply make it public by moving the `hi` method above the `private`
method in the class.

=end
